generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Snapshot {
  id        String   @id
  data      Bytes
  byteSize  Int
  createdAt DateTime @default(now())
  expiresAt DateTime

  @@index([expiresAt])
}
